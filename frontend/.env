# The API base URL is now resolved at runtime from window.location.origin
# (see src/api/axiosInstance.js). Do NOT hardcode localhost here, or the
# built bundle will point to the wrong host when deployed.
# VITE_API_BASE_URL=http://localhost:8000/api/v1